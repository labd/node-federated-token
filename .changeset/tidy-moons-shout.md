---
"@labdigital/federated-token-fastify-adapter": minor
---

Mark `fastify` and `@fastify/cookie` as peer dependencies. Their types are part of this adapter's public API, so consumers need to have them installed.
